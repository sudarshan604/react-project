import { useState, useContext, createContext } from "react";

import { Container, Frame, Inner, Title, Header, Body } from "./accordio.style";

export default Accordino({ children, ...props });
{
  return <Container></Container>;
}
